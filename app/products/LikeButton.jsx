export function LikeButton () {
    return (
        <button onClick={() => }>
            {liked ? '' : ''} 
        </button>
    )

    return (
        <button onClick={handleClick}>
            Me Gusta
        </button>
    )
mj